const makeRequestDocumentCollectives = async (req, res) => {
  try {
  } catch (error) {
    console.log(error);
    res.status(500).json({
      message: "Internal Server Error",
    });
  }
};

const uploadDocumentsCollections = async (req, res) => {
  try {
  } catch (error) {
    console.log(error);
    res.status(500).json({
      message: "Internal Server Error",
    });
  }
};

// crud documents collectives

// ada reject ada approve

// sign all documents

module.exports = {
  makeRequestDocumentCollectives,
};
